package it.polimi.ingsw.LM22.controller;

public class EffectManager {

	/*
	 * classe che si occupa della gestione di tutti gli effetti (scomuniche e Leader)
	 * che non sono standardizzabili all'interno di tipi di mosse prefissate, 
	 * oppure durante determinati controlli specifici
	 */
}
